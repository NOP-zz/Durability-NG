#include "Tools.h"


namespace Tools {
    /*
    template <typename T>
    T RandU(T max, T min) {
        thread_local std::mt19937_64 mt{ std::random_device{}() };
        return uniform_distribution<T>(min, max)(mt);
    };
    */
}
